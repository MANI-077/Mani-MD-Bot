name: Bot Keep-Alive & Maintenance

on:
  schedule:
    - cron: '*/10 * * * *' # Every 10 minutes for keep-alive
    - cron: '0 0 * * *'    # Once a day at midnight for a fresh restart
  workflow_dispatch:

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - name: Keep-Alive Ping
        run: |
          URL="${{ secrets.RENDER_EXTERNAL_URL }}"
          if [ -z "$URL" ]; then
            echo "❌ RENDER_EXTERNAL_URL secret is not set. Skipping ping."
            exit 0
          fi
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" "$URL/ping")
          echo "Ping Status: $RESPONSE"
          if [ "$RESPONSE" != "200" ]; then
            echo "⚠️ Bot seems down, triggering emergency restart..."
            curl -X POST "https://api.render.com/deploy/srv-d9kc8q3tqb8s73b88dvg?key=svdYViZ5-dk"
          fi

      - name: Daily Fresh Restart
        if: github.event.schedule == '0 0 * * *'
        run: |
          echo "🚀 Performing scheduled daily restart..."
          curl -X POST "https://api.render.com/deploy/srv-d9kc8q3tqb8s73b88dvg?key=svdYViZ5-dk"
